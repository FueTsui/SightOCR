# SightOCR 依赖（锁定版本）
#
# 为保证 PyInstaller 打包构建可复现，所有依赖钉死到经过验证的精确版本。
# PySide6 / numpy / opencv 等含原生扩展，跨大版本存在 ABI 变动，
# 用 == 锁定可避免“换台机器重装后构建产物不一致或运行时崩溃”。
#
# 升级流程：手动改高某项版本 -> 重新冒烟测试与打包验证 -> 再提交本文件。

# GUI 框架
PySide6==6.10.2

# HTTP 请求
requests==2.32.5
urllib3==2.6.3

# 剪贴板操作
pyperclip==1.11.0

# 屏幕截图
mss==10.1.0

# 图像处理
Pillow==12.1.0

# 表格识别
numpy==2.4.2
scipy==1.17.0

# 表格识别增强 (提升识别质量)
opencv-python==4.13.0.92
scikit-learn==1.8.0

# Windows API
pywin32==311

# 打包工具
pyinstaller==6.18.0
