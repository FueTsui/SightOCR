创建虚拟环境打包

```python
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
.\venv\Scripts\activate

# 更新python
python.exe -m pip install --upgrade pip

# 安装环境依赖
pip install -r requirements.txt

# 打包命令
pyinstaller --onedir --windowed --version-file=resource.rc --add-data "assets;assets" --add-data "resources;resources" --icon="assets/icon.ico" --clean --noupx SightOCR.py

# 哈希值
Get-FileHash -Algorithm SHA256 "SightOCR_v1.8.4_Setup.exe"
SHA256          4006401E82C7E51C0834E6D4703C63E672769D4BCD9FABF42F21E5CA89E67E73 
```
