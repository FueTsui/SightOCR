# 验证记录

环境：Windows x64，Rust / Cargo 1.98.1，MSVC Build Tools。记录更新日期：2026-09-06；9 月 5 日结果保留为历史记录。OCR 使用仓库测试图；原生截图测试验证取消与资源释放，不保存桌面图像。历史 Bing 在线验证仅发送合成文本 `Hello`，未使用用户云端密钥。

公开源码发布说明：OneOCR DLL/模型、真实业务表格样本及其派生 OCR 夹具只保留在本地验证工作区，不进入公开 Git 历史；公开仓库保留合成基础夹具和生产源码。下文出现的 `tests/样本`、`tests/fixtures/table-lines` 与 `target` 路径属于本地验证证据位置，不表示这些材料随源码发布。

公开 CI 使用标准 Windows Runner，未安装可选的印地语、孟加拉语语言字体，也不保证稳定的交互桌面帧；两项“已安装系统字体覆盖”测试、四项依赖完整字体栈或原生桌面渲染的 RichEdit 测试因此标记为显式环境测试并默认忽略。应在发布验证镜像上以 `cargo test --locked --bin SightOCR -- --ignored` 连同其他显式桌面测试串行运行。生产字体回退代码、纯逻辑字体测试及其他默认测试继续由 CI 执行。

## 2026-09-06 2.0.0 识别内容持续闪烁与滚轮滚动

- 原生正文此前在每个主界面帧之后强制擦除并重绘全文，即使文字、字体和布局完全未变；同时重复发送只读状态消息。改为合并实际变化，仅在内容、样式、位置、可见性、菜单遮挡或输入变化后安排刷新，并缓存只读状态。单元测试覆盖连续 120 个不变帧没有额外原生刷新，状态变化各触发一次刷新。
- 主窗口此前无条件逐帧发送 `CancelClose`，而 egui 的每条窗口命令都会请求下一帧，造成可见窗口空闲时持续刷新。现在仅在收到真实关闭请求且应用未退出时发送，保留关闭到后台和多次布局行为；连续实窗检查同时约束空闲主窗口帧数，确保停止这条循环。
- 取消显式的背景擦除，使用 `RDW_NOERASE` 抑制待处理的擦除请求；正文内容仍由原生控件正常绘制。保留识别完成、窗口恢复、主题/尺寸切换和关闭菜单后的刷新。API 行为参见 [Microsoft RedrawWindow 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-redrawwindow)。
- 验收新增独立线程的连续实窗像素采样，分别检查浅色静置、深色双栏高频界面刷新，以及日期/时间/数字/金额/标点/表格/公式和多语言混排。每组至少观察 6 秒并比较 60 帧；调试截图较慢时最多延长至 12 秒以收集足够帧数，仍不足则明确失败。读取本进程客户区已经显示的像素，不让采样线程重绘界面。比较整段正文和消失的文字像素，保存基准、差异最大帧、末帧及逐帧报告；去掉编辑光标的正常闪烁干扰。
- 静置阶段不依赖持续的界面轮询，高频阶段每 16 毫秒请求主界面刷新，并记录原文/译文的原生刷新次数。完整结果、默认测试数、实际取样帧数和新安装包校验值见 `dist/installer/VALIDATION.json`。版本仍为 **2.0.0**。
- 原生正文与主界面之间缺少滚轮路由，焦点在主窗口时滚轮只进入 egui；焦点在另一栏时也可能滚错栏。父窗口和正文现在共用按鼠标屏幕位置命中的转发逻辑，保留原始滚动量和按键状态，不改变键盘焦点。菜单遮挡和隐藏正文不参与命中；转发期间阻止递归和重复投递。
- 长文本实窗回归使用原文、译文各 100 行的日期和多语言混排，检查未聚焦正文滚动、原文聚焦时悬停译文滚动、语言菜单不穿透滚动正文，以及关闭菜单后的恢复。逐步记录真实滚动位置与消息次数，并保存五张实窗截图和报告；必须只有目标栏收到一次消息且其滚动位置增加。

## 2026-09-06 2.0.0 多语言排版、结果重绘与即时取消

- 用户最新国家列表截图的源区域经本地 OneOCR 表格模式识别，8 个阿拉伯文国名全部以完整 Unicode 返回，无替换字符或方框；证据在 `target/country-language-evidence/evidence.json`。OCR 对个别法语重音有漏识别，与界面缺字是两个问题；显示回归使用保留原重音的独立样本，未按期望答案改写 OCR 输出。
- 原文和译文改用 Windows 原生 Unicode RichEdit 控件，支持复杂文字连写、段落阅读方向、组合附标、选择和编辑。按脚本设置字体，保留文本逻辑顺序；输入与粘贴采用纯 Unicode 文本，避免公式或 RTF 开头的内容被当成富文本解析。内部字体调整保留用户光标、滚动位置和撤销记录。
- 系统字体覆盖检查包含语言菜单的 31 种语言，以及希伯来文、南亚和东南亚文字、藏文、非洲文字等额外样本；分别检查比例及等宽字体。图中的阿拉伯国名、法语重音、英语混排另经原生字体字形检查及浅色原文/深色译文视觉复核。
- 原文不做 Unicode 规范化改写。常用韩文预组合音节正常显示；本机 Windows 文字引擎将现代分解序列 `한` 显示为三个可见字母，不能把其字形视为与预组合 `한` 相同。独立 RichEdit 与 Uniscribe 对照均复现此系统行为；此边界与原先常用韩文字形缺失的方框问题分开记录。
- 识别和翻译由持久的私有子进程执行，取消会终止该进程并立即恢复界面；新的任务可马上提交，旧结果不再影响新任务。单一监督线程和最多一个待处理请求限制连续取消时的资源占用；Windows Job 在主程序异常结束时回收子进程。图片及配置经匿名管道传递，不写入命令行或临时文件。
- 取消回归包含 90 秒阻塞调用、阻塞管道写入、连续取消、新任务紧接提交、子进程崩溃恢复、退出与 Job 回收。实际程序入口还用本地代理接收 OpenAI CONNECT/TLS 后暂停响应，分别验证识别、翻译取消时连接及时关闭；未连接真实云端账号。真实 OneOCR 子进程测试验证普通/表格识别、进程复用及退出回收。
- 修复 OpenGL 主界面交换缓冲后覆盖原生正文的问题：合并投递私有窗口消息，在当前主界面绘制完成后刷新可见正文，避免黑块、识别等待页残留。隐藏控件的迟到消息不重新显示正文，原生重绘也不触发父界面重绘循环。
- 完整界面回归包含多语言原文/译文、韩文、用户国家列表、百度/腾讯公式、即时取消与新旧任务隔离，以及「等待识别 → 结果显示 → 打开语言菜单 → 切换语言」流程，共 36 个界面截图场景。
- 韩文、多语言、国家列表和公式的 8 个显示场景，以及结果/菜单切换的 3 个流程节点，直接保存本进程前台窗口客户区 DC 的实际显示像素；流程节点另存 3 张 `*-live.png`，共 39 张截图。在任何原生诊断重绘前读取，保留真实黑屏、旧画面或缺字，不用 PrintWindow 覆盖这部分证据。像素检查验证识别完成后的正文、菜单遮挡和切换语言后的正文恢复，并验证菜单打开前后的键盘焦点。其余场景使用本程序的 egui 帧缓冲与自有原生控件绘制，不采集其他窗口。最终默认测试、格式/Clippy 检查、界面报告及打包后的实际进程测试结果统一记录在 `dist/installer/VALIDATION.json`。
- 版本保持 **2.0.0**，设置「代理」名称及百度/腾讯公式块格式继续保留；安装包与主程序校验值以 `dist/installer/SHA256SUMS.txt` 为准。

## 2026-09-06 2.0.0 韩文显示与公式格式

- 对用户截图的 173×82 原文字区域运行修复前的 Release OneOCR，输出为 `你好嗎?\n안녕하세요`，韩文码点完整，无替换字符。证据保存在 `target/korean-ocr-evidence/evidence.json`；确认是界面缺少字体回退，而非 OCR 编码损坏。
- egui 原先仅加载 Segoe UI 与中文字体，不能自动使用 Windows 字体链接。现为比例和等宽字体追加韩文 Malgun Gothic（备选 Gulim/Batang）及日文字体回退，保持原有中英字体顺序，不打包系统字体。实际 egui 字形检查覆盖韩文常用音节、字母与混合中日英文本，缺少系统字体时的默认字体保留测试也通过。
- 百度、腾讯公式解析统一输出独立行的 `\[`、LaTeX、`\]`，多公式用空行分隔；避免重复外层数学定界符，保留识别正文中的空格和多行。新增回归覆盖两家服务、多公式、多行、已有定界符、空结果与无效字段，以及普通文字识别不添加公式定界符。云端响应使用本地合成数据，未调用真实账号。
- 设置导航「网络代理」改为「代理」。Cargo、安装器和程序版本保持 **2.0.0**，Windows 清单版本同步修正为 **2.0.0.0**。
- `cargo fmt --all -- --check`、全目标严格 Clippy 和默认测试通过（**142 项通过、8 项环境测试忽略**）。`target/ui-korean-formula-2.0.0/report.json` 为 `complete=true`、`errors=[]`，生成 28 张界面截图；已视觉复核韩文浅色/深色、百度/腾讯公式及紧凑代理页。原有窗口、快捷键、静默识别、截图交接与更新反馈回归通过。
- 本次发布产物及 SHA256 以 `dist/installer/VALIDATION.json` 和 `SHA256SUMS.txt` 为准；历史同版本构建记录不代表当前文件的校验值。

## 2026-09-06 2.0.0 真实表格样本回归

- 使用 `tests/样本/table1.png` 至 `table7.png`、用户提供的 Markdown 和 `table3.xlsx` 建立独立人工期望。冻结真实 OneOCR 的原图与 Rust Lanczos3 两倍输入文本框，并记录原图 SHA256；回归测试位于 `rust/ocr/table/samples.rs` 和 `tests/fixtures/table-lines/`。
- 修复表头大空白拉高全局列间距阈值、文字笔画产生假列，以及合并格文字导致局部横线丢失的问题。保留横纵线实际覆盖范围，将合并内容归入左上格；无竖线区域根据重复文字间距补列，长段无横线正文根据重复文字行补行。小数文本框只遮蔽框内像素，防止擦除紧邻边框。
- 表格模式对最长边不超过 1600、总像素不超过 150 万的输入使用两倍 Lanczos3，OCR 坐标映射回原图后重建。普通识别和大图保持原尺寸；DLL 分辨率参数不变。`table3` 中柏景灣成交数由 `M` 恢复为 `3`，未按预期数据改写 OCR 文本。
- 七张固定样本的行列结构和关键合并位置全部通过：table1 为 15×3、table2 为 24×7、table3 为 19×9、table4 为 20×5、table5 为 7×4、table6 为 8×6、table7 为 6×7。table3 的 16×9 数据矩阵和 table4 的 18×5 数据矩阵逐格核对，仅忽略 OCR 排版空白；其余样本检查结构和关键内容，不表示所有字形与数学符号均达到逐字准确。
- `scripts/test-table-samples.ps1` 从实际程序调用 OneOCR，重新生成七张图的 TSV 并验证期望，结果写入 `target/table-samples-after/report.json`。最终构建检查及 SHA256 见 `dist/installer/VALIDATION.json`。本次保留程序和安装包版本 **2.0.0**。

## 2026-09-06 2.0.0 设置与托盘文案精简

- 常规页移除「外观、识别结果、启动与托盘」分组标题，保留原有设置控件；「登录后自动启动」改为「开机自动启动」。快捷键页使用「截图翻译」，托盘使用「截图识别、截图翻译」，动作和快捷键后缀保持原有逻辑。
- 关于页删除「截图识别、文字输入、文本翻译，集中在一个轻量工作台。」和「发现新版本后将后台下载，校验完成后自动安装并重新启动。」两段静态说明；检查更新按钮、运行中的更新状态及进度继续显示。
- 格式检查、严格 Clippy 和 117 项默认自动测试通过，8 项显式环境测试保持忽略。`target/ui-settings-copy/report.json` 为 `complete=true`、`errors=[]`，共生成 24 张界面截图；已视觉复核常规、快捷键与关于页。更新进度和失败重试、快捷键录入、窗口显示及静默流程回归通过。

## 2026-09-06 2.0.0 表格识别、后台更新与自动关闭安装（历史）

本次按要求保留程序及安装包版本 **2.0.0**。使用新生成的 SHA256 区分同版本历史构建，最终文件及隔离安装报告汇总见 `dist/installer/VALIDATION.json`。

- 对比 Python 实现确认自适应阈值、多尺度形态学及 Canny/Hough 补线在重构中被简化为全局阈值长直线检测。现使用局部自适应对比、短线/小断口检测以及物理边界与文字位置融合，改善浅灰、渐变底色、缺线、只有外框的表格；保留多行单元格和空白内列，避免把词间空格与文字基线当作边界。图像处理为线性像素扫描，并限制边界和网格规模。未增加科学计算运行依赖，也不宣称完整实现倾斜/透视和复杂合并单元格识别。
- 17 项表格单测通过，包含 1600×1000 渐变背景。真实 OneOCR 两项集成回归通过，验证基础夹具、浅灰断线变体及三倍放大图的完整 TSV。默认测试共 **117 项通过、8 项忽略**；与本次相关的 OneOCR 和原生退出消息测试另行显式运行。格式检查及全目标严格 Clippy 通过。
- 关于页改为「检查更新」。后台沿用代理检查正式发布、下载并校验大小/SHA256/PE；临时更新进程等待原程序退出并再次校验后执行安装，失败尝试恢复 SightOCR，并由独立进程显示错误。19 项更新模块测试覆盖版本比较、来源及跳转限制、校验失败、截断下载、代理认证、重试、临时文件清理与更新交接恢复。测试服务仅使用本机合成数据；未发布 GitHub 或执行网络下载的安装包。
- `target/ui-table-update-2.0.0/report.json` 为 `complete=true`、`errors=[]`；24 张界面截图包含新版关于页、后台下载与失败重试，已视觉核对。新增更新进度/重试和原生安装退出消息检查通过；已有快捷键录入、截图时序、关闭后台与静默流程通过，快捷键录入未因失焦延后。
- 安装器不再要求手动退出。普通安装确认后先正常关闭目标目录内的 SightOCR，旧版兼容关闭经过明确确认；取消保留进程并退出安装。完成页默认启动，自动更新只重启一次。隔离协议测试通过确认/取消、普通静默保护、旧版兼容、异目录同名程序保留，以及等待 120 秒仍未退出时不覆盖、不强杀的负例。

## 2026-09-06 2.0.1 默认尺寸、窗口缩放与产物清理

- 默认客户区保持 740×680 逻辑像素（150% DPI 下 1110×1020），改为允许调整大小与最大化，最小客户区 740×580，不设应用级最大尺寸。移除快捷键页内重复标题及其占位，保留导航与三项按键录入。
- `target/ui-resizable-2.0.1/report.json` 为 `complete=true`、`errors=[]`。实窗检查确认默认尺寸、居中位置及可缩放/最大化的原生样式；不再由测试代码临时解锁窗口，直接验证生产窗口放大至 1060×680 和缩小至 740×580。22 张界面截图完成，已视觉核对默认工作台和无重复标题的快捷键页。
- 原有 F2 前台/后台、同帧显示后截图、取消恢复、F4/F5 的 6 项启动时序检查以及关闭后台、静默流程继续通过。输入与识别内容均为合成数据，不改真实剪贴板或访问云端。
- 版本保持 2.0.1；最终构建、安装验证和校验值见 `dist/installer/VALIDATION.json`、`dist/installer/SHA256SUMS.txt`。清理范围为旧安装包、重复版本校验文件、历史打包备份和测试安装器；历史报告与日志保留。清理记录位于 `target/cleanup-old-packages.json`，以下历史段落提到的旧安装器或备份路径可能不再存在。

## 2026-09-06 2.0.1 F2 截图时序修复与重新打包（历史）

- 定位到同一渲染帧的多次 egui 布局会累积窗口命令。旧代码在这些命令提交前启动截图线程，较早的显示/恢复命令可能在 `DwmFlush` 期间重新显示主窗口。现在先暂停主窗口动画，由 winit 提交最小化与隐藏；以真实渲染帧为界，在后一帧启动原生截图准备。窗口置前同样等待下一帧，框选期间的显示请求不抬起主窗口；最终可见性检查保留。
- `target/ui-f2-capture-2.0.1/report.json` 为 `complete=true`、`errors=[]`。新增 6 项实窗检查全部通过：F2 可见窗口、后台隐藏窗口、同帧先 Show 后 F2、取消框选，以及 F4 静默和 F5 普通识别。每次请求都验证同帧第二次布局尚未启动准备，随后真实调用原生准备与 DWM 同步，并持续观察主窗口保持隐藏。
- 测试通过平台动作事件触发生产 `start_capture` 路径；仅用合成图片/结果代替桌面采集及云端处理，用内存接收器验证复制。未逐个实际按下系统 F2/F4/F5，不读取桌面或改写用户剪贴板，真实云端翻译连接不在此回归范围。22 张界面截图及既有后台、快捷键录入、静默流程检查通过；已视觉核对关于页的版本和 FueTsui 版权信息。
- 程序与安装包保持 2.0.1，程序描述及产品名称统一为 SightOCR，第一方作者与版权统一 FueTsui，并随包提供 MIT LICENSE。旧版相同文件名安装包及其原校验信息已备份至 `target/package-backups/installer-before-f2-fix-5d17fb3bcccb4b6993f2bbfb9cdf28f6`。
- 最终构建、安装/重装/卸载检查和本次 EXE 校验值记录在 `dist/installer/VALIDATION.json` 与 `dist/installer/SHA256SUMS.txt`；以下记录属于历史构建。

## 2026-09-06 2.0.2 设置导航调整（历史）

- 设置页顺序为常规、接口、快捷键、网络代理、关于。快捷键从常规移到独立页面，按静默识别、截图识别、截图并翻译排列；离开该页或从托盘重新打开常规设置时结束录入。常规默认翻译选项及无用辅助代码已删除，语言和翻译服务仍通过工作台选择。
- `target/ui-settings-tabs-2.0.2/report.json` 为 `complete=true`、`errors=[]`，22 个界面场景输出完成。新增独立快捷键页，已视觉核对常规页、浅色快捷键页和深色窄快捷键页；按键录入、已注册热键录入和重复捕获抑制通过，未延后焦点检查。
- 保存和取消仍使用共享草稿与页底操作，代码复核确认不依赖旧常规页索引；测试不保存真实系统设置。关闭后台、独立重新激活、固定尺寸、静默/普通识别与取消回归保持通过。当时的安装包为 `dist/installer/SightOCR-Setup-2.0.2.exe`，该记录不代表当前按 2.0.1 重新打包后的校验结果。

## 2026-09-06 2.0.1 固定窗口与关闭转后台（历史）

- 关闭主窗口不再依赖 `Platform` 是否存在或托盘图标是否显示。未显式退出时每帧都发送 `CancelClose`，收到关闭请求则隐藏到后台。显式退出和重启仍释放资源并结束当前进程。
- 补充独立主窗口激活入口，避免后台服务初始化失败后隐藏窗口无法找回。已有实例优先经平台消息唤醒；平台入口缺失时只通知带标记的 SightOCR 主窗口，由 UI 消费显示意图。原生标记筛选测试及回调生命周期测试分别覆盖发送端和接收端，接收端测试已显式运行通过，验证移除、重复安装、跨线程拒绝与两种销毁顺序。
- 主窗口固定为 740×680 逻辑像素，当前 150% 缩放下客户区为 1110×1020，与参考图一致；禁用调整大小和最大化按钮，使用原文/译文分页。托盘顺序改为主界面、静默识别、识别、翻译、设置、重启、退出，动作 ID 与生效热键映射保持不变。代理页删除“按当前网络环境选择连接方式”。
- 新的实窗回归从首次关闭开始验证：先将真实托盘从显示切换为隐藏，再发送 `WM_CLOSE`；重新打开后实际停止平台线程，验证第二次关闭仍保留窗口与进程；在平台入口不存在时发送独立激活消息恢复主窗口，然后继续测试静默识别及普通识别。关闭帧强制多次布局，并检查第二次布局实际执行。
- 该回归使用生产窗口构造器验证真实 DPI 客户区尺寸、窗口不可调整和不可最大化样式，再仅在测试进程中解除尺寸限制，继续历史宽窄布局场景。报告为 `target/ui-fixed-close-final-2.0.1/report.json`；截图来自合成内容，不采集桌面或修改真实剪贴板。
- 最终实窗报告 `complete=true`、`errors=[]`，首次关闭、无平台关闭、独立重新激活、多次布局、固定尺寸以及静默流程均通过；21 个界面场景输出完成，快捷键录入未因焦点变化延后。已视觉核对固定客户区的原文/译文分页。
- 2.0.1 最终程序与安装包校验值、测试结果汇总保存在 `dist/installer/SHA256SUMS.txt` 和 `dist/installer/VALIDATION.json`。安装包为 `dist/installer/SightOCR-Setup-2.0.1.exe`。以下为此前版本的历史验证记录。

## 2026-09-06 2.0.0 结果置前、托盘隐藏与安装包（历史）

- 普通结果窗口按最终 UI 意图恢复并置前，保留原固定置顶、尺寸及最大化状态；本进程原生窗口测试 `native_bring_to_front_restores_geometry_and_window_level` 显式运行通过，覆盖隐藏/最小化/最大化、前后层级、置顶切换和非窗口线程拒绝调用。Windows 是否授予键盘前台焦点仍受系统规则控制。
- 原生事件桥只转发事件并唤醒，避免预先显示窗口。截图、关闭和退出会撤销待显示/置前操作；退出后不继续消费完成事件。隐藏托盘图标不关闭后台服务，关闭窗口和 `--silent` 启动不依赖图标可见性。
- `target/ui-background-final-v2/report.json` 为 `complete=true`、`errors=[]`。真实 `WM_CLOSE` 在禁用托盘图标的配置下隐藏主窗口，后台服务仍可用；静默成功、取消、错误和迟到事件保持隐藏，后续普通识别恢复。新增同次事件处理中的 `Show → SilentOcr` 顺序检查，未检测到静默弹窗。使用合成框选、响应和内存复制接收器，不采集桌面内容、不改用户剪贴板。
- 21 个场景输出已生成，已视觉核对浅色设置、深色窄设置、服务、代理和关于页。移除指定冗余文案与重复快捷键 tooltip，静默识别位于普通识别上方；圆角滚动条在浅深主题中均可辨认。最终运行的快捷键录入交互因失去前台焦点被明确标记延后（`hotkey_recording_deferred_for_focus=true`），上一轮 `target/ui-background-final/report.json` 中该交互通过；录入失焦保护保持启用。
- 默认测试现有 84 项通过（库 69、界面 15），6 项默认忽略；构建脚本执行格式检查、严格 Clippy、默认测试和 Release 构建。界面代码与原生恢复逻辑均经独立审查。
- Inno Setup 6.7.3 编译器已通过官方 SHA-256 和发布者签名校验，便携部署在工作区。安装器按当前用户安装、包含模型与文档、不强制关闭已运行程序，保留用户配置。隔离验证覆盖两次安装、负载逐文件哈希、包内 OneOCR 表格识别、静默不启动、卸载与合成配置保留；具体报告及范围见 [安装说明](INSTALLATION.md)。正式安装包为 `dist/installer/SightOCR-Setup-2.0.0.exe`，最终文件校验值记录于同目录 `SHA256SUMS.txt`。SightOCR 安装包未配置代码签名。

## 2026-09-06 静默识别与三组快捷键

默认 F4 静默识别、F5 普通识别、F2 截图并翻译。常规设置提供三组独立按键录入，托盘新增静默识别；关于页删除退出按钮。静默识别完成后复制原文并保持窗口隐藏，取消及错误不自动弹窗；主动打开主界面和后续普通任务仍正常显示。窗口激活统一在 UI 线程按任务编号决定，过期及重复的结束事件不再处理。

- 格式检查和严格 Clippy 通过；默认自动测试 83 项通过（库 69、界面 14），5 项默认忽略。
- 新增测试覆盖旧 F4 默认配置迁移、自定义按键优先保留、新静默键避开配置内重复、显式保存后不再迁移、三组等价组合冲突检查，以及静默录入仅修改对应草稿字段。
- 原生热键集成测试显式运行通过：三组注册、第三组冲突回滚、交换后的动作路由、`WM_HOTKEY` 静默事件及退出后注销释放。七项托盘顺序和已生效快捷键标签检查通过。
- 修复托盘关闭后“已隐藏但未最小化”被误判为窗口重新显示的问题：已隐藏窗口不再执行多余的最小化操作，最终以不可见作为准备成功条件。原生测试验证隐藏未最小化与隐藏已最小化两种状态，重复准备后位置、尺寸及原最小化状态均保持不变。
- `target/ui-silent-final/report.json`：21 个界面截图完成，`complete=true`、`errors=[]`。静默流程从真实主窗口的托盘隐藏、未最小化状态开始；成功、Esc、框选失败、识别失败、取消后通知、旧/重复事件均保持隐藏，复制内容与次数符合预期；随后普通识别恢复窗口。静默任务的提交快照明确禁止翻译，忙碌中的其他热键不会改写该快照。
- 已视觉核对宽窄设置页：移除大标题“设置”，返回与导航同一行，底部仅保留“取消”和“保存”，三项快捷键可见；关于页已无退出按钮。录入中状态及已注册按键重复抑制本次实窗检查通过，`hotkey_recording_deferred_for_focus=false`。前两次运行因用户前台焦点变化而中止；测试器保留失焦保护，并单独标记此类交互检查的延后状态，不将其伪报为通过。
- 复制验证使用独立内存接收器，未覆盖用户剪贴板；验证的是提交复制的原文和次数。真实运行继续使用 egui/Windows 剪贴板通道，实际系统剪贴板被其他进程独占时的写入不在本次模拟验证范围。框选图和识别响应均为测试合成数据，没有读取桌面内容或调用云服务。
- `scripts/build.ps1` 完整通过，发布包内 EXE 与模型在隔离临时配置下识别测试表格，得到 `Name\tAmount`、`Alpha\t100`、`Beta\t200`，退出码 0。便携目录仅含程序、文档及三个模型/引擎资源。
- 此历史版本 EXE SHA-256：`0FA0F6CAB7C9F1B20B3A2474F5B304E9B9213AA231FA610450641F5A2E6ADED9`。以下各节为此前版本的验证记录。

## 2026-09-06 快捷键录入与截图窗口交互

本次追加快捷键按键录入、截图前隐藏主窗口与框选完成后恢复等待页、初始窗口在当前显示器工作区居中，以及「返回」按钮和「SightOCR」托盘悬停提示。

- 严格 Clippy 通过。默认自动测试 77 项通过：库 65 项，界面程序 12 项（7 项按键录入、4 项原生窗口、1 项模式映射）；5 项默认忽略，范围同下节说明。
- `target/ui-proxy-progress/report.json` 最新报告为 `complete=true`、`errors=[]`，21 个界面截图生成完成。按键录入可捕获已经注册的全局快捷键，同时抑制由该按键触发的重复截图；已视觉核对录入中的蓝色状态及设置页「返回」文案。
- 原生窗口门控验证截图前最小化并隐藏、截图流程结束后恢复窗口；初始窗口在当前显示器工作区居中检查通过。工具栏、全局快捷键和托盘共用截图路径已代码复核，本轮未逐项人工点击入口或保存桌面截图验证像素排除。此前原生框选 Esc 取消及 GDI/USER 资源释放检查通过。
- 修复窗口恢复后 egui 焦点状态未及时更新、导致已注册按键无法录入的问题，改以原生前台窗口状态判定。截图的最小化和隐藏交由同一窗口流程控制，避免重复执行造成状态竞争。
- 旧进度过滤、取消、显示、设置及重启回归检查全部通过。快捷键仍在保存成功后生效并同步标签；「返回」和仅显示「SightOCR」的托盘悬停文案已同步。
- `scripts/build.ps1` 最终执行通过格式检查、严格 Clippy、77 项自动测试及 Release 构建。使用便携包 EXE、包内模型和隔离临时配置识别 `tests/fixtures/basic.png --table`，得到 `Name\tAmount`、`Alpha\t100`、`Beta\t200`，退出码 0。
- 最终 EXE SHA-256：`08FDEFA6EBF34CD26AB08EC0D2EE0F822B25012828F4B9F21DD9D1F2121819E4`。发布目录仅含程序、三个引擎/模型资源及说明文档。多显示器混合 DPI 尚未在真实硬件验证。

下节保留追加前 66 项默认自动测试、20 个界面场景的阶段记录；当前总数以上述 77 项和 21 个场景为准。

## 2026-09-06 网络代理、等待页与框选绘制

本轮增加独立网络代理设置页：跟随系统、不使用代理、手动代理；支持 HTTP、HTTPS、SOCKS5、SOCKS5H 及单独填写认证用户名/密码。系统模式优先读取环境代理变量，再读取 Windows 手动代理设置，不执行 PAC/WPAD。代理只影响 SightOCR 的云端识别/翻译，不修改系统设置，保存后对新任务生效。

等待页新增真实任务阶段、提交时服务与目标语言快照、计时和取消入口；截图并翻译在 OCR 完成后先展示原文，继续等待译文。取消或任务编号不匹配时丢弃迟到的进度与完成事件。

框选按旧 `src/screenshot.py` 恢复 `#FFFFFF`、1px 白边，选区外一半亮度、选区内清晰。绘制复用两张 DIB，原图只读；在离屏预览缓冲中完成合成，每次绘制只提交一次完整更新区域，避免分层绘制的中间帧闪烁。

- 格式检查、严格 Clippy（所有 targets、`-D warnings`）通过。
- 默认自动测试 66 项通过（库 65、界面 1），5 项默认忽略。忽略项含 4 项原生/DLL 测试及 1 项由父测试在隔离子进程中调用的环境代理测试辅助入口。
- 本机模拟测试验证 HTTP CONNECT、独立代理认证、全部云服务入口使用代理、直连、代理更换后的会话清理、SOCKS5 本地 DNS、SOCKS5H 远端 DNS/RFC1929 认证、环境代理继承、非法代理配置及迁移。测试不连接真实云服务。首次运行发现 Windows 接受的测试 socket 继承非阻塞标志，修正 mock 后完整测试通过。
- 合成像素测试验证白边、框外半亮与框内原图、扩大/缩小/反向拖动、贴边/零宽、脏区域合并、旧边框清除，以及原始截图保持不变。原生截图两轮 Esc 取消及 GDI/USER 对象释放检查通过。
- 实际 OneOCR DLL 测试验证 OCR 原文通过阶段事件发布；在翻译阶段通知时取消后，不再初始化 HTTP 或发送原文。
- `target/ui-proxy-progress/report.json`：20 个真实界面截图完成，`complete=true`、`errors=[]`。已视觉检查代理宽窄窗口、识别等待、翻译等待、深色最小窗口叠加回退警告、长时间等待及取消状态，取消和保存按钮完整可见。
- 阶段事件提前显示原文、取消后忽略迟到进度、隐藏窗口不被进度唤醒，以及原有完成/显示/设置/重启事件回归均通过。修复等待图标背景被拉伸和居中布局意外移动状态栏的问题。
- 最新 Release 便携包及包内 OCR 验证结果见上节。以下 9 月 5 日的截图和校验和仅作为历史记录。

## 服务与交互扩展历史验证（2026-09-05）

该轮增加 Mistral OCR、OpenAI/NVIDIA OCR 与翻译；各服务拥有独立密钥和 Base URL，OpenAI/NVIDIA 分别选择识别与翻译模型。默认模型分别为 `mistral-ocr-latest`、`gpt-4.1-mini` 和 `qwen/qwen3.5-122b-a10b`。官方请求/响应协议已经核对，尚未使用新增服务的真实密钥发起请求。

界面移除图片导入和拖放识别，命令行 `--ocr` 继续保留。工具栏以图钉切换置顶、齿轮直达设置；外观及默认翻译语言/服务移入常规设置，七项云服务通过选择器切换。托盘新增完整六项菜单：主界面、识别、翻译、设置、重启、退出；快捷键标签仅在保存成功后同步更新。

- 格式检查和严格 Clippy（`--all-targets --locked -- -D warnings`）通过。
- 默认自动测试 51 项通过（库 50 项、界面模式映射 1 项），4 项硬件/原生集成测试默认忽略。新增测试覆盖三家服务的请求格式、授权头、响应解析、Mistral 单列及多列表格、配置迁移、错误与重定向、输入/输出大小限制、取消和接口路由；HTTP 请求仅发送到本机模拟服务器。
- 该轮另行显式运行实际热键冲突/交换/回滚/注销测试，以及 OneOCR 完成后取消、不再启动翻译的 DLL 集成测试，均通过。
- 原生托盘菜单测试验证六项顺序、命令 ID、当前快捷键标签和菜单释放；事务测试验证更新成功后标签变化、失败时保留旧标签、启动注册失败不显示无效快捷键。
- `target/ui-api-tray/report.json`：14 个真实窗口截图均已生成，`complete=true`，`errors=[]`。已视觉检查主界面、常规设置、OpenAI 服务表单、NVIDIA 最小窗口表单；服务内容独立滚动，保存/取消按钮保持可见。
- 隐藏窗口的显示、设置事件以及完成/取消结果处理均通过。重启测试验证请求取消任务并关闭应用；测试运行器不会实际重启进程。重启实现经复核，先完成应用/热键资源释放，再释放单实例锁并启动新进程。
- `scripts/build.ps1` 完整执行通过，生成新版 `dist/SightOCR/SightOCR.exe`。使用发布包内的 EXE 和模型、独立临时配置执行 `--ocr tests/fixtures/basic.png --table`，得到 `Name\tAmount`、`Alpha\t100`、`Beta\t200`，退出码 0。
- 新版 EXE 的 SHA-256：`3F9654D8730354C0B80AAC3504497E0F018B59BF7EED3F202403A2B30A5342E8`。新增云接口未使用真实账号密钥验证连通性和识别质量；上述界面截图为合成结果，不表示已调用云服务。

## 此前已通过：历史基线（2026-09-05）

- 格式检查与严格 Clippy（`-D warnings`）通过。
- 离线默认测试：34 项通过；默认忽略的 4 项原生/DLL 集成测试在 Rust 重构阶段全部显式运行通过。此次光标修改后再次运行原生截图取消测试通过。
- 实际 OneOCR DLL：同线程连续两轮初始化、文本识别、表格识别及释放通过，识别夹具中的 `Alpha`、`100`，表格包含 `Alpha\t100`。
- 原生热键：实际注册、制造冲突、回滚、交换动作、注销和窗口销毁通过。
- 原生截图：两轮自动 Esc 取消通过，测试前后进程 GDI 对象数完全一致。
- 识别完成后的取消：原文可读取，翻译客户端未初始化，未继续发送请求。
- PowerShell 启动/构建脚本语法检查通过。
- Release CLI 的文本、TSV 表格、中文图片路径和不存在文件错误返回通过。
- 实际 GUI 冒烟测试通过：主界面/设置渲染、首帧后隐藏、后台完成唤醒、取消结果保持隐藏、显示事件恢复。`target/ui-smoke/report.json` 的检查项均为 true，errors 为空。
- 上一轮 Release 便携目录 `dist/SightOCR` 由 `scripts/build.ps1` 完整生成，内置版本为 2.0.0；使用包内 EXE 和模型识别测试表格通过。
- 发布目录仅包含 EXE、说明文档和三个模型/引擎资源，无 `config.json`、`paths.json`、日志或 Python 文件。
- 上一轮 Rust Release 的默认 Bing 在线翻译：独立空配置，合成文本 `Hello` → `你好`，退出码 0。旧 Edge 授权 404 已修复。

GUI 测试应串行运行：一次与其他程序验证并行的测试在设置页之前提前退出，未形成完整报告；最终独立运行完整通过。主界面和设置页截图已进行视觉检查。

便携程序位置：`dist/SightOCR/SightOCR.exe`；同目录的 `resources/oneocr` 必须随程序保留。`启动.bat` 已切换至 Rust 启动脚本。

## 截图工具风格 UI 历史验证（2026-09-05）

- `target/ui-snipping/report.json`：11 种界面截图全部生成，启动后隐藏、后台完成唤醒、取消结果丢弃并保持隐藏、显示事件恢复均通过，`complete=true`，无错误。
- 已视觉检查浅色/深色、识别/翻译空白状态、原译文对照、设置及服务页、窄窗口与740×580最小窗口的错误和处理中状态。文本独立滚动，复制/翻译/取消及保存按钮保留在可见区域。
- 工作台改为单行工具栏与居中空白提示，无侧边导航。翻译空白状态可选择目标语言和服务。
- 修复错误提示撑开布局、卡片和工具栏边界裁切、处理期间覆盖用户编辑、失败/取消后残留错误图片尺寸、清空后保留旧错误提示等问题。
- 截图仅含合成内容和临时测试配置，未操作用户云端密钥或桌面内容。实际网络服务及多显示器验证范围沿用下列限制。
- 关于页真实 Logo 截图通过；窗口、程序和托盘使用用户指定的 icon.png/icon.ico。截图光标嵌入原始 cross.png，原生内存绘制测试验证32×32尺寸、16/16热点及逐像素透明合成；两轮取消截图后 GDI 与 USER/光标对象数均无增长。

## 尚需目标环境验证

- 百度、腾讯、Mistral、OpenAI、NVIDIA 真实账号的服务权限、配额、模型可用性及在线识别/翻译；Mistral 仅用于 OCR。
- Bing 网页兼容接口在其他网络环境中的可用性，以及未来服务协议变化。
- 实际代理部署中的路由、认证、系统代理来源和网络兼容性；PAC/WPAD 不在支持范围内。
- 多物理显示器混合 DPI、远程桌面、锁屏/睡眠恢复。
- 正式安装包在真实开始菜单、安装登记、运行实例冲突、旧 Python 版升级及其他 Windows 环境中的完整交互验收；编译及隔离安装/覆盖/卸载已经验证。
- 与旧 Python 版对比的启动速度、峰值内存和复杂表格识别率基准。
